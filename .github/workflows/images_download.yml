name: images downloader

on:
  push:
    branches:
      - master

jobs:
  build_docs_and_deploy:
    runs-on: ubuntu-latest
    name: build up document and deploy

    steps:
    - name: Checkout
      uses: actions/checkout@master

    - name: Clone pre repositories
      run: |
        ls
        echo '------------------------------'
        git config --global user.email "ucecwme@ucl.ac.uk"
        git config --global user.name "nickcafferry"
        git init
        cd ./source/assets/images/mopy4bslides/
        
        ls
        echo '============================================'
        
        mkdir Cleanup_with_with
        cd Cleanup_with_with/
        wget -O Cleanup_with_with.zip https://onlineconvertfree.com/upload/converter/030920/5NB66z3/08_-_Cleanup_with_with.zip
        unzip Cleanup_with_with.zip
        git add -A
        git rm -f Cleanup_with_with.zip
  
        cd ../../../../../
        git status 
        git commit -m "Add some photos "
        ls
        git push origin master
